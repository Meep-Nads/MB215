# Activity 5: Using Constants Variables
# Define a constant variable, i.e. PI
PI = 3.14

#radius value as float from user. 
radius = float(input('What is the radius of the circle?'))

# use constant variable to area or a circle.
Area = PI * (radius ** 2)
print(f'The area of the circle is: {Area}')
